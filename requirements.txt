# Galgame 场景编辑器依赖
# 主要依赖库
PyQt5==5.15.9

# 用于 JSON 数据处理（内置）
# json

# 用于文件操作（内置）
# os, sys, shutil, pathlib

# 可选：根据项目需求可能需要的其他依赖
# pillow==9.5.0  # 用于图像处理增强
# numpy==1.24.3  # 用于数值计算
# pyyaml==6.0.1  # 用于 YAML 配置文件